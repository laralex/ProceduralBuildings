<UserControl x:Class="WindowsGeneratorView.SegmentingProperties"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WindowsGeneratorView"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <Label Content="Segmenting settings" 
                   FontWeight="Bold" FontSize="16"/>
        <local:IntervalField MinValue="{Binding MinNumberOfFloors}" 
               MaxValue="{Binding MaxNumberOfFloors}" 
               LabelValue="Floors" 
               FilterType="Integer" TextBoxWidth="70"/>
        <local:IntervalField MinValue="{Binding MinSelectedWallHorizontalSegments}" 
               MaxValue="{Binding MaxSelectedWallHorizontalSegments}" 
               LabelValue="Horizontal segments on selected wall" 
               FilterType="Integer" TextBoxWidth="70"/>
    </StackPanel>
</UserControl>
